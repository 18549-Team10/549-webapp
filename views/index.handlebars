<head>
	<title>StreamFi | Home</title>
	<link rel="stylesheet" type="text/css" href="//fonts.googleapis.com/css?family=Open+Sans">
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
	<link rel="stylesheet" href="/css/index.css">
	<script src="https://code.jquery.com/jquery-3.2.1.js"></script>
</head>

<body id="background">
	<div id="left-nav">
		<ul>
		  <li><a class="active" href="#"><i class="fa fa-home"></i><br>Home</a></li>
		  <li><a href="#"><i class="fa fa-tint"></i><br>Containers</a></li>
		  <li><a href="#" onclick="findValues()"><i class="fa fa-bar-chart"></i><br>Statistics</a></li>
		  <li><a href="#"><i class="fa fa-cog"></i><br>Settings</a></li>
		  <li><a href="#"><i class="fa fa-sign-out"></i><br>Sign Out</a></li>
		</ul>
	</div>
	<div id="content" >
		<h2>Hello World!!!</h2>
	</div>
	<div id="json-data">
	</div>

	<script>

	function findValues() {
		var myList;
		$.getJSON('/json/test.json').done(function (data) {
			myList = data;
			var jsonData = document.getElementById("content");
			jsonData.innerHTML += data.firstName;
			console.log(data);
		});
	}
/*		function findValues() {
			var treeData;

			var oReq = new XMLHttpRequest();
			oReq.onload = reqListener;
			oReq.open("get", "/json/test.json", true);
			oReq.send();

			function reqListener(e) {
			    treeData = JSON.parse(this.responseText);
			    console.log(treeData);
				var theDiv = document.getElementById("json-data");
				var content = document.createTextNode(treeData.firstName);
				theDiv.appendChild(content);
			    console.log(theDiv);
			}
		}*/
	</script>
</body>